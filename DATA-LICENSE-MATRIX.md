# XFINLAB Intelligence API v1 — Data License Matrix

**Purpose:** before scaling XFINLAB's Intelligence API into the primary commercial product ("API-first" pivot), this maps every *paid* `/intelligence/v1/*` endpoint to the upstream data source(s) it actually depends on, and that source's real license status.

**Source of truth:** this file does not introduce new judgments about licensing — it rolls up what already exists in `services/license_registry.py` and `services/source_registry.py` (both already in this repo, last updated 2026-08-10) and maps it onto the *specific endpoints being sold*, which neither of those files does directly. If a source's status changes, update `license_registry.py` first; this file should be regenerated from it, not edited independently.

**2026-08-11 update (RESOLVED):** `ALPACA_API_KEY_ID` / `ALPACA_API_SECRET_KEY` were absent from production, confirmed live via Railway logs showing every price/OHLC-touching endpoint 100% yfinance-backed. AJ has since signed up for Alpaca and set both keys in Railway's Variables tab. Verification along the way surfaced a second, unrelated bug: this app never called `logging.basicConfig()` anywhere, so every `logger.info()` call across the whole codebase (not just this diagnostic) was silently swallowed by Python's default WARNING-only last-resort handler — uvicorn's own access-log lines were unaffected (uvicorn configures its own loggers independently), which made the app look like it was logging normally while it wasn't. Fixed in `backend/main.py` (commit `50b0d67`). With that fixed, production logs now confirm: **`Alpaca served OHLC for AAPL (6mo, 1d)`** — Alpaca is live and serving US-symbol requests as of 2026-08-11. Every row below marked "confirmed live (Alpaca never fires)" is now stale and superseded — see the per-endpoint table for the corrected status.

---

## Per-endpoint breakdown

| Endpoint | Weight | Service chain | Upstream source(s) | Worst-case license status | Risk |
|---|---|---|---|---|---|
| `GET /v1/events` | 1x | `rss_news_service` | globenewswire_rss (public domain), prnewswire_rss (public domain), gdelt (public domain — **replaced investing_com_rss 2026-08-11**) | clean — no non-commercial-licensed source in this pool anymore | **low** |
| `GET /v1/sentiment` | 1x | `rss_news_service` (headlines) + `finbert_sentiment_service` | same clean pool as above + huggingface_inference_api (commercial) | clean | **low** |
| `GET /v1/debate` | 5x | `agent_debate_service` (+ `technical_analysis_service` for context) | LLM providers (Claude/Groq/DeepSeek/OpenRouter/Kimi — standard paid commercial APIs, not a data-license risk) + Alpaca/TWSE/yfinance chain for price context | alpaca_markets (US, confirmed live), twse_official (Taiwan `.TW`, confirmed live 2026-08-11), yahoo_finance fallback (other non-US or on error) | low (US + Taiwan symbols), high (other non-US symbols, e.g. HK) |
| `GET /v1/intel/latest`, `GET /v1/intel/{ticker}` | 8x | `intelligence_pipeline_service` → `news_dedup_engine`, `ai_news_object_service`, `news_impact_engine`, `ai_journalist_service`, `event_chain_service` | clean rss_news_service pool (globenewswire/prnewswire/gdelt) + `market_data_gateway` → `technical_analysis_service` (Alpaca/TWSE-first, confirmed live) + finbert + LLM | clean news leg; OHLC leg alpaca_markets (US) / twse_official (Taiwan) / yahoo_finance (other non-US) | **low** (US + Taiwan), **high** for other non-US symbols only — no longer high across the board |
| `GET /v1/technical/{ticker}` | 3x | `technical_analysis_service` | Alpaca confirmed live in production 2026-08-11 (`Alpaca served OHLC for AAPL (6mo, 1d)` in Railway logs) for US symbols → **TWSE confirmed live 2026-08-11 (`TWSE served OHLC for 2330.TW`) for Taiwan symbols** → yfinance fallback for other non-US symbols or on error | alpaca_markets (US, confirmed), twse_official (Taiwan, confirmed), yahoo_finance (other non-US) | low (US symbols), low (Taiwan symbols), high (other non-US, e.g. HK) |
| `POST /v1/stress-test` | 3x | `monte_carlo_service` → `fetch_ohlc_history` | same — Alpaca-first (US) / TWSE-first (Taiwan) confirmed live | alpaca_markets (US, confirmed), twse_official (Taiwan, confirmed), yahoo_finance (other non-US) | low (US symbols), low (Taiwan symbols), high (other non-US) |
| `GET /v1/regime-signal/{ticker}` | 3x | `regime_router_service` → `backtest_service` + `formula_composer_service` → `technical_analysis_service` | same — Alpaca-first (US) / TWSE-first (Taiwan) confirmed live | alpaca_markets (US, confirmed), twse_official (Taiwan, confirmed), yahoo_finance (other non-US) | low (US symbols), low (Taiwan symbols), high (other non-US) |
| `GET /v1/world/market-map` | 6x | `world_engine_service` | finbert (huggingface, low) + `global_news_region_service` (rss pool — no longer includes investing_com_rss, now globenewswire/prnewswire/gdelt + BBC RSS, low) + `macro_data_service` (World Bank, public domain, low) | clean news pool; BBC RSS still unverified (unknown/medium, see `bbc_rss` in `license_registry.py`, added 2026-08-11) | **low–medium**, down from high — only remaining caveat is the still-unverified BBC RSS leg |
| `GET /v1/world/regions` | — | `world_engine_service.list_regions` | static, no external call | — | none |

## What this confirms

Every endpoint that touches raw price/OHLC data (`technical`, `stress-test`, `regime-signal`, and the price-context leg of `debate` and `intel/*`) now uses a commercially-clean source-first chain: Alpaca (confirmed live via production logs, 2026-08-11) for US-listed symbols, **TWSE's official STOCK_DAY OpenAPI (confirmed live 2026-08-11) for Taiwan-listed (`.TW`) symbols**, yfinance fallback only for everything else (other non-US markets, e.g. Hong Kong, or on any upstream error). That risk is now resolved for both US and Taiwan symbols.

The investing.com RSS exposure — previously the single biggest open item, affecting `events`/`sentiment`/`intel/*`/`world/market-map` regardless of symbol — is **resolved as of 2026-08-11**. `investing_com_rss` was removed entirely from `rss_news_service.py`'s feed pool (see `services/license_registry.py`'s entry, confirmed `non_commercial`/`high` risk via direct fetch of Fusion Media's footer terms) and replaced with `gdelt` (public-domain, unlimited commercial use, already integrated elsewhere in this codebase, broader real-time global coverage than a single wire's RSS feed). The remaining pool — `globenewswire_rss`, `prnewswire_rss`, `gdelt` — is entirely public-domain/unrestricted-commercial-use. The only residual caveat across the whole news-fed endpoint group is `bbc_rss`, still flagged `unknown/medium` pending independent verification (see action items below).

`market_data_service.py` (the fully-yfinance, unmitigated `/api/market` and `/api/analyze` consumer endpoints) is confirmed **not** used by any Intelligence API v1 endpoint — its risk is real but belongs to the consumer surface that this pivot is de-emphasizing anyway, not the product being sold.

## Action items, in priority order

~~Sign up for Alpaca, set `ALPACA_API_KEY_ID`/`ALPACA_API_SECRET_KEY` in Railway.~~ **Done 2026-08-11**, confirmed live via production logs.
~~Spot-check that Alpaca is actually being used, not just configured.~~ **Done 2026-08-11** — confirmed via `Alpaca served OHLC for AAPL (6mo, 1d)` in Railway logs. (Along the way this surfaced and fixed an unrelated bug: the app never called `logging.basicConfig()`, so all `.info()`-level logs anywhere in the codebase were silently invisible — see `backend/main.py` commit `50b0d67`.)
~~Verify `investing_com_rss`'s actual redistribution terms.~~ **Done 2026-08-11** — confirmed high risk, see above.
~~Add a BBC RSS entry to `services/license_registry.py`.~~ **Done 2026-08-11** — added as `bbc_rss`, flagged `unknown/medium`. Could not be independently verified: bbc.co.uk is blocked for this environment's fetch tool, and web search only surfaced an old (~2007) report of planned RSS-term changes, not the actual current terms. Needs a human to check `bbc.co.uk/usingthebbc/terms/using-rss-feeds-from-the-bbc/` directly.
~~Fix the investing_com_rss exposure.~~ **Done 2026-08-11** — removed from `rss_news_service.py`'s feed pool entirely, replaced with `gdelt` (public-domain, already-integrated, broader coverage than a same-size RSS substitute). `get_all_headlines()` now merges GDELT's macro feed; `search_headlines()` now also calls GDELT's server-side search, deduplicated by link against the existing RSS-pool substring match. Accepted tradeoff, per AJ ("要後者係穩陣選擇但會少啲新聞覆蓋面"): general-market breaking-news coverage investing.com used to provide is not fully replaced by press-release wires + GDELT, in exchange for zero license risk.
~~Wire TWSE OHLC into the technical-analysis fetch chain for Taiwan symbols.~~ **Done 2026-08-11**, per AJ's explicit authorization ("冇接落technical analysis嗰條OHLC chain,要接嘅話係新工程，接啦"). `technical_analysis_service.py::_fetch_history()` now tries TWSE's STOCK_DAY endpoint first for any `NNNN.TW` symbol on daily interval (TWSE has no intraday data, so intraday requests go straight to yfinance same as before), looping over calendar months and concatenating, with month-level caching (indefinite for closed months, 15-min TTL for the current month). Falls back to yfinance on any error/empty result, same defensive non-hard-fail pattern as the Alpaca integration. Confirmed live via production logs: `TWSE served OHLC for 2330.TW (...)`. `services/license_registry.py`'s `twse_official` entry updated to document the expanded usage.

1. **Hong Kong non-US-symbol gap remains open.** HKEX's own official historical data (`hkex.com.hk/eng/ods`) is a paid subscription product, not a free API — confirmed by direct fetch, the site is literally a subscribe-and-pay portal. No free official alternative surfaced in research. Realistic options are: pay for an HKEX data license, find and vet a commercial-tier third-party provider, or keep the current explicit scope decision that HK-listed tickers are yfinance-backed (documented, not hidden) until/unless that changes. Current honest scope statement: *"XFINLAB Intelligence API v1 guarantees commercially-licensed data sourcing for US-listed and Taiwan-listed symbols today; Hong Kong and other non-US markets currently use a best-effort fallback source pending a licensing decision."*
2. **BBC RSS terms still need independent human verification** (see above) — low urgency, `world/market-map` is already down to low–medium risk overall with this as the only open leg.
3. Now that the investing.com and Taiwan gaps are both closed, the marketing copy on `intelligence-api.html` and any "Financial Intelligence Infrastructure" positioning should say plainly which endpoints are commercially clean end-to-end (everything except non-US-non-Taiwan OHLC and the residual BBC RSS caveat) — a real differentiator against competitors who don't disclose this at all, not just a liability shield.

This document does not replace legal review before large-scale commercial launch — it establishes the factual baseline (what actually calls what) so that a lawyer, if/when engaged, is reviewing real usage instead of guesses.
